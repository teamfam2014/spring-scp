scp:
    keyFilePath: ./docker/ssh_host_rsa_key.pub
    userName: root
    host: localhost
    port: 22
    localFilePath: ./
    remoteFilePath: /tmp
    sessionConfigs:
        StrictHostKeyChecking: "no"